# B 站官方动态资讯接入

用户已授权实施。先验证异环官方 UID `3546636978489848`，回补最近 60 天。

## 约束

- 仅入资讯，不自动修改活动日历；视频动态及转发视频一律排除。
- 排除抽奖/开奖、PV/EP、实机演示、时装展示等宣传动态。
- 正文必须包含可定位的日期，并与版本更新、游戏内活动或卡池直接相关；发布时间不充当活动时间，图片不自动 OCR。
- 保存原文、原链接、筛选结论和原因；不把删除/异常/风控响应当作空列表成功。
- 来源独立保存，失败保留已采集记录；不影响原有公告、游戏账号与提醒。

## 实现与验证

1. `sources/bilibili.py`：SDK 客户端、动态规范化、规则筛选、60 天有界分页；测试视频/宣传/抽奖/确定日期/置顶/分页/异常。
2. `sources/bilibili_store.py`：SQLite 独立资讯及审计记录、来源状态，按 UID + 动态 ID 幂等写入；不覆盖原有快照。
3. `sources/bilibili_service.py`：单任务锁、定时增量查询、手动回补、退避，异环 UID 默认启用；API 查询与刷新。
4. API 将 B 站资讯合并到游戏 `news` 快照并标明来源；前端增加来源标识及 B 站采集状态、回补入口和筛选审计展示。
5. 真实拉取该 UID 最近 60 天，与规则筛选输出逐条核对、保留报告；运行后端/前端测试、构建、重启、检查页面。

匿名 SDK 获取已实测成功；不导入外部机器人推送代码，不执行点赞或视频下载。
