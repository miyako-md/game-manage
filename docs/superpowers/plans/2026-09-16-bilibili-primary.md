# B站主来源实施计划

目标：按用户确认的方向，鸣潮、异环的资讯与版本日历优先采用B站官方动态，社区补缺和回退。沿用现有独立B站存储、60天回补、加密登录及10分钟增量采集。

设计依据：`docs/bilibili-calendar-validation.md` 的真实样本，以及用户“主要资讯和版本活动从B站拉取，社区补充”的指示。

- [x] 新增纯函数模块 `sources/public_content.py`：官方正文标题归一化；游戏专用分段解析；当前已开放版本选择；总公告与独立通知合并；B站与社区去重合并。用 `tests/test_public_content.py` 先验证相对时间、跨行日期、异环省略年份、未来版本排除、不同阶段保留和来源优先级。
- [x] 接入 `bilibili_service.py` 与 `api.py`：资讯和事件快照提供B站优先合并结果；来源失败保留原记录并标记过期；社区账号错误不影响公共来源状态。保留原社区快照。启用鸣潮官方UID1955897084；现有异环UID不变。
- [x] 前端调整 `calendar.js`、`CalendarPage.vue`、`dashboard.js`：B站资讯优先与跨频道去重；按日期精度展示相对开始时间，详情提供原始时间、来源链接及冲突说明；来源面板显示正确游戏名。
- [x] 用已采集公开原文回放测试，并通过真实后台回补验证数量、来源、去重与社区补缺；运行相关后端和前端测试、构建、重启及浏览器检查。更新项目说明。

约束：不将维护预计结束时间当作实际开服时间；未知时间不是00:00；不将新版本前瞻当作当前版本；同名不同时间阶段不合并；未配置B站UID的游戏沿用现有来源。禁止输出凭据。原日历与登录信息不删除。只在B站有明确当前版本依据时补充同版本社区事件，避免旧版本混入。

验收：后端558 passed / 4 skipped，前端77 passed，生产构建成功；服务重启后健康检查通过，浏览器确认烟云赠礼展示相对开始、日期精度、B站来源与原文链接。独立审查提出的延期、相对日期冲突、未来版本和多期时间问题均已修复并回归。仅修改手游来源逻辑。
